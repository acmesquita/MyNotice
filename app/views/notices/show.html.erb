
<div class="container">
	<div class="jumbotron">
	<p id="notice"><%= notice %></p>

	<h2>
	  <strong> <%= @notice.title %> </strong> - <%= @notice.criation %>
	</h2>

	<p>
	  <%= @notice.body %>
	</p>

	<%= link_to 'Editar', edit_notice_path(@notice), class:"btn btn-primary" %>
	<%= link_to 'Voltar', notices_path,  class:"btn btn-default"%>
	</div>
</div>
